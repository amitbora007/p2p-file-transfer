services:
  - type: web
    name: p2p-signaling-server
    env: node
    plan: free
    buildCommand: npx pnpm install && npx pnpm run build:server
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
